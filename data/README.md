# Data Layout

This project keeps the main processed datasets directly in the repository.

Directory usage:

- `data/raw/`: local source files such as `data.xlsx`
- `data/processed/vae/`: VAE-ready processed dataset
- `data/processed/tfidf/`: TF-IDF-ready processed dataset and feature names

If needed, you can regenerate the processed data locally from the raw source files.
