# Output Layout

This project keeps core experiment outputs directly in the repository.

Typical outputs include:

- `outputs/vae/`: plots, reports, checkpoints, and explainability artifacts
- `outputs/tfidf/`: baseline reports and serialized pipelines
- `outputs/reports/`: summary reports and exported analysis text files
- `outputs/legacy/`: preserved local historical exports when needed
